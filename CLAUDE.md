@.claude/memory/MEMORY.md

## Commit rules

- Commit after every meaningful change — don't batch unrelated work into one commit.
- Commit messages must be brief: a short conventional-commits title only (e.g. `feat(landing): accountants page`). No body, no bullet lists.
- Never add `Co-Authored-By` or any Claude/AI attribution to commits.
